// Package schema parses TiDB CREATE TABLE snapshots into an immutable,
// offline physical-schema catalog.
//
// It does not provide a schema DSL, execute SQL, or connect to a database.
package schema
